# LR-13

# Лабораторная работа №13: Мультиагентные системы: разработка распределённых интеллектуальных агентов

Автор: Гармашов Виталий Валерьевич  
Группа: 221331  
Вариант: 17  
Предметная область: Кибербезопасность (SIEM)  
Уровень: задания повышенной сложности

## Общая информация

- **Дата выполнения:** 21.05.2026
- **Цель:** реализовать распределённую мультиагентную SIEM-систему с Go-агентами, Python-оркестратором, NATS, Redis, Jaeger, API, тестами и документацией.
- **Методология:** Agentic Engineering: анализ методички, проектирование архитектуры, итеративная реализация, тестирование, end-to-end проверка, документирование.

---

## Этап 1: Анализ задания и выбор архитектуры

### Промпт 1

**Инструмент:** Codex

**Промпт:** «Нужно выполнить лабораторную работу №13 в соответствии с требованиями. Вариант 17. Выполняем задания повышенной сложности. Просмотри работу. Если появятся вопросы, задавай. Работаем в Python.»

**Результат:** Из PDF выделены требования лабораторной работы. Установлено, что вариант 17 относится к предметной области «Кибербезопасность (SIEM)» и требует агентов: сбор логов, корреляция событий, детекция атак, блокировка трафика.

**Проверка:** Методичка прочитана, найден блок заданий повышенной сложности.

**Коммит:** не выполнялся

**Время:** ~10 мин

---

## Этап 2: Уточнение технологического стека

### Промпт 2

**Инструмент:** Codex

**Промпт:** «Давай делать так, как указано в задании. Оставить агентов на Go, а Python использовать только для оркестратора/API.»

**Результат:** Зафиксирован стек: Go для микросервисов-агентов, Python/FastAPI для оркестратора и API, NATS как брокер сообщений, Redis для состояния, Jaeger/OpenTelemetry для трассировки.

**Проверка:** Проверено наличие `go`, `docker`, `python` в окружении.

**Коммит:** не выполнялся

**Время:** ~5 мин

---

## Этап 3: Реализация Go-агентов

### Промпт 3

**Инструмент:** Codex

**Промпт:** «Собери полноценный каркас лабораторной: Go-агенты, Docker Compose, тесты и документацию под SIEM-вариант 17.»

**Результат:** Созданы 4 Go-агента:

- `agents/log_collector` — нормализация сырых логов;
- `agents/correlator` — корреляция событий;
- `agents/detector` — детекция атак и аукционный bid;
- `agents/blocker` — блокировка IP и сохранение состояния в Redis.

**Проверка:** Выполнено форматирование `gofmt`, позже подтверждено через `go test ./...`.

**Коммит:** не выполнялся

**Время:** ~45 мин

---

## Этап 4: Реализация Python-оркестратора и API

### Промпт 4

**Инструмент:** Codex

**Промпт:** «Python использовать только для оркестратора/API.»

**Результат:** Реализованы:

- `orchestrator/core.py` — запуск pipeline, ожидание результата, retry, timeout, audit events;
- `orchestrator/api.py` — FastAPI endpoints `/tasks/siem`, `/status`, `/`;
- `orchestrator/models.py` — Pydantic-модели задач, результатов и bid;
- `web/templates/dashboard.html` — простая веб-панель мониторинга.

**Проверка:** Оркестратор проверен pytest с моками NATS.

**Коммит:** не выполнялся

**Время:** ~35 мин

---

## Этап 5: Инфраструктура Docker Compose

### Промпт 5

**Инструмент:** Codex

**Промпт:** «Добавь Docker Compose, Redis/Jaeger/NATS и скрипты запуска.»

**Результат:** Созданы:

- `docker-compose.yml` для NATS, Redis, Jaeger, FastAPI и 4 Go-агентов;
- `agents/Dockerfile` для сборки Go-агентов;
- `Dockerfile.python` для Python API/LLM-агента;
- `scripts/run_demo.ps1` для демонстрационного запроса;
- `scripts/scale_agents.py` для масштабирования `attack-detector`.

**Проверка:** `docker compose config` успешно прошёл, `docker compose build` собрал все образы.

**Коммит:** не выполнялся

**Время:** ~30 мин

---

## Этап 6: Тесты

### Промпт 6

**Инструмент:** Codex

**Промпт:** «Не забудь про тесты (pytest и т.д.). Нужно чтобы всё работало корректно.»

**Результат:** Добавлены:

- `internal/siem/rules_test.go` — Go unit-тесты нормализации логов, корреляции и детекции;
- `tests/test_orchestrator.py` — pytest для успешного pipeline и retry/timeout;
- `pytest.ini` — настройки pytest и asyncio.

**Проверка:** Выполнены команды:

```powershell
go test ./...
python -m pytest
```

Обе проверки прошли успешно.

**Коммит:** не выполнялся

**Время:** ~25 мин

---

## Этап 7: Исправление зависимостей и сборки

### Промпт 7

**Инструмент:** Codex

**Промпт:** «Проверь сборку/тесты и поправь всё, что всплывёт.»

**Результат:** Исправлен импорт OpenTelemetry exporter в Go:

```text
go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracehttp
```

После исправления выполнен `go mod tidy`, создан `go.sum`, зависимости успешно скачаны.

**Проверка:** `go test ./...` прошёл без ошибок.

**Коммит:** не выполнялся

**Время:** ~20 мин

---

## Этап 8: End-to-end проверка

### Промпт 8

**Инструмент:** Codex

**Промпт:** «Нужно чтобы всё работало корректно.»

**Результат:** Поднята система через Docker Compose и выполнен демо-запрос:

```powershell
docker compose up -d
.\scripts\run_demo.ps1
```

Pipeline успешно прошёл через всех агентов. Результат: атака определена как `high-risk intrusion`, IP `203.0.113.17` заблокирован.

**Проверка:** Проверены:

- `/status` в FastAPI;
- наличие `203.0.113.17` в Redis `siem:blocked_ips`;
- статус контейнеров через `docker compose ps`.

**Коммит:** не выполнялся

**Время:** ~20 мин

---

## Этап 9: Документация

### Промпт 9

**Инструмент:** Codex

**Промпт:** «Создай README с названием работы, вариантом, выполненными заданиями повышенной сложности, группой 221331 и ФИО Гармашов Виталий Валерьевич.»

**Результат:** README оформлен под лабораторную №13: добавлены название из методички, автор, группа, вариант, цель, архитектура, выполненные 8 заданий повышенной сложности, запуск, тесты и структура проекта.

**Проверка:** Проверено наличие всех обязательных данных для сдачи.

**Коммит:** не выполнялся

**Время:** ~15 мин

---

## Этап 10: Финальная подготовка репозитория

### Промпт 10

**Инструмент:** Codex

**Промпт:** «Добавь PROMPT_LOG.md и .gitignore со всем необходимым.»

**Результат:** Созданы:

- `PROMPT_LOG.md` — журнал промптов и этапов разработки;
- `.gitignore` — исключения для Python, Go, Docker, логов, кэшей, IDE и локальных секретов.

**Проверка:** Файлы добавлены в корень проекта.

**Коммит:** не выполнялся

**Время:** ~10 мин

---

## Общий итог

### Количество крупных этапов: 10

### Основные инструменты

- Codex
- Go
- Python/FastAPI
- NATS
- Redis
- Jaeger/OpenTelemetry
- Docker Compose
- pytest

### Что пришлось исправлять итерационно

1. Уточнить стек: Go-агенты обязательны по методичке, Python используется для оркестратора/API.
2. Исправить Go-путь OpenTelemetry exporter.
3. Установить Python-зависимости перед запуском pytest.
4. Добавить `asyncio_default_fixture_loop_scope = function`, чтобы убрать предупреждение pytest-asyncio.
5. Проверить не только unit-тесты, но и реальный Docker Compose pipeline.

### Проверенные команды

```powershell
go test ./...
python -m pytest
docker compose config
docker compose build
docker compose up -d
.\scripts\run_demo.ps1
```

### Итоговая проверка

- Go-тесты: успешно
- Python-тесты: `2 passed`
- Docker build: успешно
- End-to-end demo: успешно
- Redis state: IP `203.0.113.17` сохранён в `siem:blocked_ips`

